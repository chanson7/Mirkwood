using UnityEngine;
using UnityEngine.InputSystem;

public class PredictedAnimationMotion : MonoBehaviour
{

    // Animator animator;
    // PredictedTransform predictedTransform;

    void Start()
    {
        // animator = this.GetComponent<Animator>();
        // predictedTransform = this.GetComponent<PredictedTransform>();
    }

    public StatePayload ProcessAnimationMotion(StatePayload statePayload)
    {
        return statePayload;
    }

}
